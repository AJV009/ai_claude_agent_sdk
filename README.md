Start
## Local Development Setup

This module owns the Claude SDK local setup script:

`scripts/setup-claudeagentsdk-dev.sh`

Run it via the project command:

`ddev setup-claudeagentsdk-dev`

The DDEV command is only a wrapper that ensures this module exists and then executes the module script.
